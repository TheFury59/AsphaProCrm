# Aspha Pro — Lessons

Format : `[YYYY-MM-DD] | ce qui a mal tourné | règle pour l'éviter`

- [2026-05-12] | shadcn 4.x exige Tailwind v4 (installation initiale v3 a planté) | toujours vérifier la matrice de compat shadcn × Tailwind avant l'install
- [2026-05-12] | CORS `allowed_origins: ['*']` incompatible avec `supports_credentials: true` (browser rejette les cookies) | utiliser `allowed_origins_patterns: ['/.*/']` en dev avec credentials
- [2026-05-12] | `Relation::enforceMorphMap()` bloque Spatie ActivityLog (User fait des morphs auto) | utiliser `Relation::morphMap()` (sans enforce)
- [2026-05-12] | `DateTime::createFromFormat` retourne `false` avec Carbon → Factur-X cassé | utiliser `new \DateTime((string) $value)` quand la source est Carbon
- [2026-05-12] | `exception_date` stockée en datetime UTC (22:00Z = 18/05 Paris) ne matchait pas la clé YYYY-MM-DD | `Carbon::parse(...)->toDateString()` pour les clés d'exception
- [2026-05-12] | shape Eloquent `clientCompanies` HasMany alors qu'on attend `company` HasOne | patcher le model après génération automatique DBML, et propager le rename dans tous les eager loads
- [2026-05-12] | PowerShell `ConvertTo-Json` + `Invoke-WebRequest` encoding chelou en UTF-16 BOM | passer du JSON brut en string littérale plutôt que via hashtable convertie
- [2026-05-12] | Notifications table avait `notification_type_id`/`target_type`/`target_id` (pas `type`/`data`) | aligner systématiquement le type TypeScript sur la migration réelle, pas sur l'assomption "Laravel notifications standard"
- [2026-05-12] | Migration `make:migration create_vehicles_tables` génère un boilerplate `Schema::create('vehicles_tables')` qu'il faut écraser entièrement | toujours relire la migration générée avant de la sur-écrire, et utiliser `Read` avant `Write` (l'outil l'impose)
- [2026-05-12] | `MessageThreadParticipant` pas généré par les scripts DBML-to-models (composite pk) | créer manuellement les models pivot quand on doit en lire/écrire les colonnes indépendamment (cas last_read_at)
- [2026-05-12] | `MapPage` utilisait `addr.line1` / `addr.lat` qui n'existent pas | la migration Address réelle a `address`, `latitude`, `longitude` — toujours vérifier la migration avant d'écrire un composant qui consomme la ressource
- [2026-05-13] | Drag-and-drop FullCalendar décalait les RDV de 2h (15h→13h) à cause du round-trip TZ | NE PAS utiliser `Date.toISOString()` pour POSTer une datetime (toujours UTC) quand le backend est en `APP_TIMEZONE=Europe/Paris` + SQLite (qui perd la TZ). Toujours envoyer en local naïf `"YYYY-MM-DDTHH:MM:SS"` sans Z ni offset → Carbon parse en TZ applicative, round-trip stable.
- [2026-05-13] | Recalculer `occurrence_date` depuis la NOUVELLE date après drag d'une occurrence virtuelle | sinon l'exception est liée à l'ancienne date et n'efface pas la bonne occurrence. Utiliser `arg.event.start.toLocaleDateString("sv-SE")` (format ISO YYYY-MM-DD local).
- [2026-05-13] | `replace_all: true` sur `where('user_id', ...)` a aussi remplacé les occurrences sur `Employee` (qui doit garder `user_id`) | méfiance avec replace_all sur identifiants partiels — préférer des edits ciblés avec contexte unique quand 2 entités proches partagent le même champ
- [2026-05-13] | FullCalendar ne fournit pas de hook contextMenu | wrapper le calendar dans un `<div onContextMenu>` et lire `data-time`/`data-date` du slot ciblé pour récupérer la date du clic
- [2026-05-13] | Settings paramétrables : préférer table `app_settings` JSON-value typée à des constantes hardcodées | permet à l'admin de modifier seuils (45 min, 5j absence, etc.) sans déploiement. Cache 60s pour ne pas hit BDD à chaque request
- [2026-05-13] | Validation Laravel rejetait `employee_id: null` avec `validation.exists` | la règle `exists:` valide null aussi tant qu'on n'a pas ajouté `nullable` explicite. Toujours combiner `['sometimes', 'nullable', 'exists:...']` sur les FK optionnelles
- [2026-05-13] | `owner_user_id` sur table employees était NOT NULL sans default → violation contrainte lors d'une création | défaut auto = utilisateur courant côté controller pour les champs FK "gestionnaire" obligatoires
- [2026-05-13] | Géocodage BAN échouait silencieusement sur Windows (cURL error 60 SSL) | PHP Windows n'a pas bundle CA → `Http::withOptions(['verify' => false])` uniquement quand `app()->environment('local')`. Garder strict en prod Linux
- [2026-05-13] | `Cache::remember()` cachait aussi les échecs `null` pour 30j → adresse corrigée restait bloquée | gérer le cache manuellement (Cache::get/put) et NE PAS cacher les échecs, seulement les succès
- [2026-05-13] | `L.circle(center).getBounds()` crash avec `layerPointToLatLng undefined` quand le circle n'est pas attaché à une map active | calculer les bounds manuellement via formule géo (1° lat ≈ 111 km, 1° lng ≈ 111 km × cos(lat))
- [2026-05-13] | Cache invalidation React Query : mutation Intervention n'invalidait que `["interventions"]`, pas `["planning"]` → panneaux trajets/contrats stale | helper `invalidatePlanningRelated()` qui invalide en batch (interventions + planning + matching) sur create/update/delete/exception
- [2026-05-13] | DialogContent shadcn applique `sm:max-w-sm` par défaut → override `max-w-[1280px]` (sans préfixe) ignoré au-dessus de sm | utiliser `sm:!max-w-[1280px]` avec `!` (important Tailwind) pour forcer l'override
- [2026-05-13] | Rules of Hooks violées : `useMemo` après early returns conditionnels → "Rendered more hooks than during the previous render" | TOUS les hooks doivent être appelés AVANT tout early return, sans exception. Avec fallback (null/0) si les data ne sont pas chargées
- [2026-05-13] | Filter `where('status', 'active')` sur Employee alors que la colonne n'existe pas en BDD | toujours vérifier la migration avant d'écrire un filter. Sur SQLite l'erreur "no such column" arrive direct en 500 alors qu'en MySQL elle peut être silencieuse selon le mode strict
- [2026-05-13] | `<Circle>` Leaflet crash en mutation in-place du `radius` prop sur certains états transitoires | `key={radiusKm}` force un remount complet propre à chaque changement
- [2026-05-13] | ClientController/EmployeeController index ne faisaient pas `with('addresses')` → MapPage affichait "aucune adresse géocodée" alors que les coords étaient en BDD | toujours eager-load les relations utilisées en consommation directe sur les listes
- [2026-05-15] | Path frontend `client?.client_companies?.[0]?.company_name` sur 3 pages alors que le backend retourne `client.company` (singular HasOne) | quand on patche un model auto-généré DBML pour transformer HasMany en HasOne, propager la rename dans TOUS les consommateurs frontend (grep nécessaire car TS n'aide pas — `any` masque)
- [2026-05-15] | Devis créés sans items (pas de table quote_items mirror invoice_items) → devis "vides" | quand on copy-paste un pattern (Invoice items inline), créer aussi la migration table miroir + relation, pas juste le dialog UI
- [2026-05-15] | Cascade d'invalidation React Query : mutations addresses/contacts d'un client n'invalidaient pas le calendar (qui eager-load ces données) | helper `invalidateClientCascade` qui invalide `['client', id, sub]` + `['clients']` + `['interventions']` + `['planning']` en batch sur chaque mutation
- [2026-05-15] | `TypeError: rows.filter is not a function` sur QuotesListPage parce que le hook retournait la pagination Laravel double-wrappée (`['data' => $paginate]` + `paginate.data = [...]`) | toujours **normaliser** côté hook avec un helper `unwrapPaginated<T>()` qui détecte le double-wrap et retourne `{ data: T[], meta: {...} }` cohérent — sinon les composants finissent par accéder à un objet au lieu d'un array et plantent au runtime
- [2026-05-15] | `ClientMissionsTab` listait `client_prestations` à plat sans grouper par mission, alors que la BDD a une hiérarchie 1:N `missions → client_prestations` qui matérialise le contrat client | quand le schéma a une relation parent-enfant **structurante métier** (pas juste un FK de référence), l'UI doit la refléter dès le départ : cartes expandables parent + enfants imbriqués, dialogs séparés "créer parent" puis "ajouter enfant"
- [2026-05-15] | Avais codé la création de mission en simple Dialog alors que Xelya/Ximi a une PAGE dédiée avec batch create mission+prestations | quand l'utilisateur réfère un outil concurrent ("comme sur Xelya"), TOUJOURS relire le DBML/spec pour aligner exactement les champs et valeurs d'énum (ex: `payment_methods // 'check,sepa,card,transfer,cash'` — ne pas inventer ses propres labels). Dialog = ajout enfant à parent existant. Page = création parent + N enfants en transaction.
- [2026-05-18] | Saisir un ID humainement dans un champ texte est insupportable pour l'utilisateur ("super chiant") — pattern present dans 3 endroits (messagerie, tickets, missions) | toute selection d'entite (client, intervenant, mission, prestation) doit passer par un picker visuel avec photo/logo + recherche serveur ; les ID restent uniquement dans le payload api, jamais dans l'UI
- [2026-05-18] | Notifications affichees toutes identiques rendent impossible la priorisation visuelle | mapper notification_type.code -> icone + couleur + module dans `lib/notification-styles.ts`, border-left coloree + badge module + lien "Ouvrir" contextuel selon target_type ; eager-load la relation notificationType cote backend
- [2026-05-18] | URL d'image avec mise a jour : sans cache-buster le browser garde l'ancien fichier en cache local apres remplacement | accessor Laravel ajoute toujours `?v={updated_at.timestamp}` a l'URL du fichier ; le navigateur recharge a chaque update car la query string change
- [2026-05-18] | Double création de modules pour la même donnée (ici tickets sur fiche client ET page admin globale, 2 UI séparées) avec mismatch silencieux sur les types/champs → contenu perdu sans erreur visible | quand on découvre une feature dupliquée pour la même table, **vérifier mismatch validation backend ↔ payload frontend en priorité absolue** (les enums Laravel `in:...` rejettent les valeurs invalides mais sans message visible côté UX si la mutation a un onError générique). Unifier ensuite le flow : même endpoint OU même hook + cascade invalidation sur toutes les vues qui lisent la table
- [2026-05-18] | Emission manuelle de notification dans 4 controllers + observer Eloquent existant pour le même event → double notif silencieuse | un seul point d'émission : Observer Eloquent sur `created`/`updated`/etc., jamais d'appel direct dans les controllers. Garantit DRY + cohérence quelle que soit la route de création (admin, portail, extranet, import, command artisan)
- [2026-05-18] | `$target::class` retourne le FQCN Laravel ("App\Models\ClientRequest") qui pollue le frontend, contrairement à `$target->getMorphClass()` qui respecte la morphMap | dans tout dispatcher de notification ou résolution polymorphe, **toujours `getMorphClass()` + déclarer le morph map** pour stocker un code court ("client_request") exploitable côté UI sans regex
- [2026-05-18] | Bug semantique grave : `clients.owner_user_id` (= gestionnaire Aspha) servait aussi a identifier le user client de l'extranet -> un admin gestionnaire devenait "le client" de tous ses dossiers | toujours **separer les roles d'une FK** quand ils ont des semantiques differentes, meme si techniquement on pourrait re-utiliser. Une colonne dediee `portal_user_id` cout 0 et evite l'ambiguite + permet aux deux roles de coexister sans conflit
- [2026-05-18] | Mot de passe en clair retourne via API : tentation de le stocker ou re-fetcher | NE JAMAIS persister le clair, l'afficher one-shot dans un dialog avec bouton copy + warning "ne sera plus jamais affiche", et toute action "re-send email" doit obligatoirement regenerer un nouveau password (sinon faille de securite ou erreur 500 cherche du clair inexistant)
- [2026-05-18] | 4 routes pointaient vers le meme composant -> les boutons de menu changeaient l'URL mais affichaient la meme page (illusion "ne fonctionne pas") | quand on creee un layout avec onglets multiples, **chaque tab DOIT avoir sa propre page component** (sinon decouvre brutalement le bug une fois en prod). Pattern : extraire les sections en composants exportes + 1 page wrapper par route, qui compose les sections necessaires. Evite la duplication tout en garantissant la navigation visuelle
- [2026-05-18] | Code de modules entiers (SMS, Yousign) developpe puis abandonne -> dette si pas nettoye | quand l'utilisateur tranche "on en veut pas", retirer dans la foulee : jobs + service + table colonnes (via migration drop safe avec down()) + config services + seeder + frontend UI + types. La table en BDD reste si destructive (rollback safe), mais le code mort partout est traque et supprime. Documenter la decision avec date dans les commentaires des fichiers retires
- [2026-05-18] | FK fantome dans une migration (`constrained('contacts')` au lieu de `client_contacts`) -> tout INSERT echouait silencieusement sur SQLite avec "no such table: main.contacts" | TOUJOURS verifier les `constrained()` quand le nom de la table FK n'est pas le pluriel canonique du modele : `client_contacts` != `contacts`, `creditor_contacts` != `contacts`. SQLite n'erreur PAS a la migration (FK creee avec table inexistante) mais a l'INSERT. Validation Laravel `exists:contacts,id` aussi a corriger pour matcher
- [2026-05-18] | `await mutateAsync()` sans try/catch -> dialog reste ouvert, pas de toast, user croit que le bouton ne marche pas | toute mutation submit DOIT etre dans try/catch avec : (1) toast.error + premier message de validation Laravel via `data?.errors[..][0]`, (2) console.error avec payload + response complet pour debug, (3) toast.success + onClose() UNIQUEMENT au succes. C'est plus important que d'avoir un beau code, c'est ce qui evite "rien ne se passe" en demo cliente
- [2026-05-18] | SQLite refuse `dropColumn` quand une FK fantome existe ("error in table interventions after drop column: unknown column in foreign key definition") | hack SQLite : `PRAGMA writable_schema=ON` + `UPDATE sqlite_master SET sql = REPLACE(...)` pour patcher la definition stockee + `PRAGMA writable_schema=OFF`. Pour MySQL/Postgres `dropConstrainedForeignId + foreignId+constrained` passe sans soucis. La migration doit detecter le driver et adapter
- [2026-05-18] | Bug FK fantome propage : la migration ET le model avaient le meme bug "contact" au lieu de "client_contact". Apres fix de la FK, le model continuait a planter en chargeant la relation | quand on corrige un bug de nommage de table (FK/migration), TOUJOURS verifier en parallele : (1) la relation Eloquent (`belongsTo(...::class)`), (2) la validation Laravel (`exists:...`), (3) les eager-loads (`->with('relation')`). 3 endroits = 3 bugs separes a corriger sinon on traite que la moitie
- [2026-05-18] | Handler `onError: () => arg.revert()` SANS toast -> l'event FullCalendar revient en place silencieusement, impression que le drag-drop est casse alors que c'est juste l'API qui erreur | TOUS les onError de mutations doivent afficher un toast.error explicite. Pattern : `onError: (err) => { toast.error(err?.response?.data?.message ?? "fallback"); console.error(...); arg.revert(); }`. Idem qu'avec le bouton submit silencieux : meme regle, partout
- [2026-05-18] | Un service qui utilise `relationLoaded()` pour resolution conditionnelle est silencieusement KO si le controller appelant ne fait pas les bons eager-loads | quand un service expose un methode qui prend `Collection $items` (au lieu de charger lui-meme), il a une DEPENDANCE IMPLICITE sur les eager-loads de l'appelant. Soit (a) le service charge lui-meme (le path-A par defaut de InterventionExpander), soit (b) le controller appelant DOIT eager-load le meme set que ce que le service charge. Documenter la dependance dans le PHPDoc du service ou idealement, faire un wrapper `Intervention::scopeWithExpanderLoads()` reutilisable
- [2026-05-18] | `invalidateQueries({queryKey})` sans `refetchType: 'active'` -> les queries restent "stale" sans refetch immediat -> impression "ne s'actualise pas" apres une mutation | pour les calendars / dashboards / listes visibles a l'ecran apres une mutation, TOUJOURS passer `refetchType: 'active'` (ou utiliser refetchQueries directement). Par default React Query attend le prochain mount/focus -> UX terrible
- [2026-05-18] | useState dans un Dialog qui ne reset pas quand les props changent -> le user clique sur un slot, ouvre le wizard, mais c'est l'ancienne date pre-remplie | quand un composant a son `useState` initialisé depuis des props et que le dialog peut se ré-ouvrir avec d'autres props sans démonter, AJOUTER une `key={prop?.id}` pour forcer le remount + ré-init. Alternative : useEffect(() => setForm(...), [prop]) mais c'est plus fragile et duplique le initializer
- [2026-05-18] | `Date.toISOString().slice(0,10)` converti en UTC -> peut donner une date différente près de minuit (ex: 23h Paris = 21h UTC le lendemain = OFF BY ONE DAY) | pour extraire la date locale d'un objet Date, TOUJOURS construire à la main : `${y}-${MM}-${dd}` avec getFullYear/getMonth/getDate. `toISOString()` = UTC, dangereux pour les dates locales. Idem pour toTimeString
- [2026-05-18] | `<button>` ou `<Button>` shadcn dans un `<form>` sans `type="button"` -> implicitement `type="submit"` -> click submit le form -> creation accidentelle | dans tout composant qui PEUT etre utilise dans un form (filtres, toggles, picker buttons, etc.), TOUJOURS ajouter `type="button"` explicite. Pattern strict : "tout button qui n'est pas le submit principal du form a `type='button'`". Une fois la regle violee dans un coin, c'est facile de la propager
- [2026-05-18] | FullCalendar `select` emet arg.end = arg.start + slotDuration pour un clic simple. En zoom serre slotDuration peut etre 5 min -> RDV de 5 min cree par defaut | detecter clic vs drag : `arg.end - arg.start <= 15 min` = clic simple -> forcer 1h. Sinon = drag = respecter la selection. Seuil 15 min car les slots FullCalendar courants sont 5-15 min ; un vrai drag depasse toujours
- [2026-05-18] | Pattern "click pour selectionner + bouton dedie pour confirmer" -> les users cliquent et oublient le bouton de confirmation -> form incomplet silencieux. Cas reel : map de selection intervenant avec etape "Affecter" en 2eme clic | preferer l'**auto-confirm au clic** quand le bouton de confirmation est redondant avec la selection visuelle. Une bannière de feedback (✓ Affecte) replace le bouton. Le user peut toujours desaffecter via un bouton "Retirer" dans une autre section. Plus court, plus intuitif, plus difficile a oublier
- [2026-05-18] | Bouton submit `disabled={!allStepsDone}` -> le user clique, rien ne se passe visuellement, il croit que c'est un bug -> abandonne | **NE PAS** disable le submit pour les regles metier. Laisser cliquable -> au click, validateForm() liste explicitement les erreurs dans un toast + highlight visuel des sections concernees (anneau rouge, badge "à compléter"). Seul disable acceptable = `isPending` (eviter double-submit). C'est la SEULE facon que le user comprenne ce qu'il rate
- [2026-05-18] | Erreur SQL brute affichée dans l'UI ("SQLSTATE[HY000]: no such column") = horrible pour le user ET exposition de la structure BDD aux attaquants | Laravel 11 `bootstrap/app.php withExceptions` -> render() pour QueryException : log details serveur avec ref courte 8 chars, renvoie 500 JSON avec message user-friendly + error_ref. UNE seule modification couvre toute l'API, pas besoin de try/catch partout. Bonus securite : ne fuite plus les noms de colonnes ni les FK
- [2026-05-18] | Migration initiale "oubliee" de creer une colonne pourtant referencee partout dans le controller/model -> bug silencieux jusqu'a la 1ere mutation | quand on cree controller+model+migration en parallele, faire un `grep` final de toutes les colonnes referencees dans le controller (->where, ->update, fillable, casts) vs la migration pour detecter les ecarts. Idealement un test feature minimal qui INSERT au moins une fois chaque table apres les migrations
- [2026-05-18] | Seeder de demo qui assumait une structure de table mais le schema reel etait different (client_contacts : type/value vs first_name/last_name) -> erreur a la 1ere insertion | TOUJOURS verifier la migration AVANT de coder un seeder qui insert, surtout pour les tables sub-resources qu'on touche moins souvent. Pattern : ouvrir la migration cible dans un onglet, coder le seeder a cote
- [2026-05-18] | Detection de conflits horaires en temps reel : appel API a chaque touche sur input date/heure -> bombardement backend | hook debounce 500ms via setTimeout dans useEffect avec cleanup. Pattern reutilisable : sérialiser params en JSON pour la key deps, debounce le call, silent fail si l'API echoue (le backend re-valide a la mutation finale). Cle UX : non-bloquant = warning qu'on peut bypass, bloquant = confirm() obligatoire
- [2026-05-18] | Feature de validation (check-conflict) implementee dans le wizard de creation mais oubliee sur le drag-and-drop -> meme RDV avec conflit possible apres drag sans avertissement | quand on implemente une regle metier (conflit, validation, calcul), lister TOUS les points d'entree de mutation : creation (wizard), update (dialog edit), drag-drop (handleEventDrop/Resize), exception (recurrence), API externes futures. Cibler chaque point. Sinon une feature "marche" en demo mais le bug ressurgit via un autre chemin
- [2026-05-18] | Action user qui declenche un async + UI modale qui apparait apres -> fenêtre de race condition où le user peut commencer une 2eme action qui plante. Cas FullCalendar : drag-drop A puis drag-drop B avant que la pop-up de A apparaisse -> calendar bloqué | TOUJOURS verrouiller l'UI pendant TOUTE la fenêtre (depuis l'event jusqu'à la résolution complète, pas juste pendant l'affichage modal). Pattern : state `isProcessing` set true AVANT l'await, false dans finally + state `dialog !== null` -> `locked = isProcessing || dialogOpen`. Visuel : opacity + pointer-events-none sur le wrapper, ET désactivation native des features (editable/selectable de la lib si dispo)
- [2026-05-18] | Bouton "Mon profil" dans le dropdown ne menait nulle part — onClick manquant -> dropdown se ferme silencieusement | quand on crée un dropdown menu avec des items "feature à venir", soit (a) ne pas les afficher du tout, soit (b) les disable visuellement, soit (c) leur ajouter onClick + page minimale même placeholder. JAMAIS un item cliquable qui ne fait rien : l'utilisateur croit que c'est cassé
- [2026-05-18] | Garde-fou anti-lockout super_admin : empêcher un super_admin de se rétrograder OU désactiver lui-même | quand on code un système de rôles/permissions, TOUJOURS prévoir le scénario "le dernier super_admin se vire lui-même par erreur". Check `if ($user->id === $request->user()->id && newRole !== 'super_admin')` côté backend + bouton disabled côté frontend avec tooltip explicatif
- [2026-05-18] | Page admin/users n'affichait que 2 users -> question légitime "pourquoi ?" parce que UI ne donnait pas le contexte | quand un système a 2 concepts liés mais distincts (User Laravel vs Employee/Client entité métier), AFFICHER LE CONTEXTE en haut de la page admin : bandeau explicatif qui précise le scope et oriente vers les bons flow pour les cas non couverts. Sans contexte, l'utilisateur croit que c'est un bug
- [2026-05-18] | Forcer le user à viser un bouton "Ouvrir" précis dans chaque ligne d'un tableau = friction inutile | composant `ClickableRow` réutilisable qui rend la ligne entière cliquable, avec garde-fou `e.target.closest('a,button,[role=button]')` pour préserver les actions locales (badges client cliquables, boutons actions). Pattern à appliquer SYSTEMATIQUEMENT sur toutes les listes de l'app, sans exception
- [2026-05-18] | Composant `PortalAccessCard` taillé pour Client uniquement, duplication évidente quand on a voulu l'employer pour Intervenant -> tentation de copier-coller 250 lignes | refactor en composant générique avec prop `type: EntityType` + labels paramétrables via `DEFAULT_LABELS[type]`. Hooks parallèles aussi : `useCreatePortalAccess(type, id)`. Quand 2 entités partagent le MEME flow métier mais avec des textes différents, paramétrer >> dupliquer. Idem pour les endpoints backend : 2 controllers parallèles (Client + Employee) acceptables car ils touchent des FK différentes (portal_user_id vs user_id)
- [2026-05-18] | Endpoint extranet permettant création de ticket par intervenant pour n'importe quel client = faille de spoofing évidente (l'intervenant peut envoyer un ticket pour un client qu'il ne connaît pas) | TOUJOURS vérifier côté backend que l'entité cible (client) a un rapport légitime avec l'utilisateur connecté (au moins 1 intervention chez ce client). 403 si non. Pattern reusable : "filtrer le picker côté UI" + "garde-fou backend redondant"
- [2026-05-18] | Refacto composant générique "PortalAccessCard" mais les sous-composants (CreateAccessDialog) restaient avec des labels hardcodés "client" -> bug visible immédiatement en ouvrant le dialog côté intervenant | quand on paramètre un composant pour 2 entités, lister TOUS les sous-composants ET vérifier qu'aucun label visible ne reste hardcodé. Pattern : grep `"au client"|"du client"|"client"` dans le fichier après refacto pour traquer les oublis
- [2026-05-18] | Ajout d'un nouveau champ au modèle + migration + validation request + frontend, mais oubli du `$request->only([...])` dans le controller qui filtrait silencieusement -> PATCH 200 OK mais champ jamais sauvé | quand on ajoute un nouveau champ à un modèle Laravel, checklist obligatoire : (1) migration, (2) fillable du model, (3) FormRequest validation, (4) controller `$request->only(...)` whitelist OU passer en `$request->validated()` qui ne whitelist que les champs validés -> évite l'oubli. Préférer `validated()` à `only(...)` justement pour ça
- [2026-05-19] | `empty($bc)` PHP renvoie `true` pour `['civility' => null]` -> vider 1 champ d'un sous-objet via PATCH déclenchait un DELETE complet de la relation (billing_contact perdu) | NE JAMAIS utiliser `empty()` pour décider d'une DELETE en cascade : un payload partiel envoyé par l'UI peut contenir des null/string vides légitimes. Distinguer "absence du champ" (`! has`) de "passé à vide" (`array_filter`). Pour une purge intentionnelle, exiger au moins 2 champs tous nuls/vides pour confirmer l'intention
- [2026-05-19] | `updateOrCreate(['client_id' => id], $data)` crée un enregistrement sans valider que les champs required du model y sont -> ClientCompany sans company_name | TOUJOURS vérifier en amont si la création est légitime quand updateOrCreate touche un sous-objet avec colonnes NOT NULL non garanties par le payload partiel (ex: PATCH `{company: {phone: "..."}}` sans company_name). Garde explicite : `if (! $hasIt && empty($data['required_field'])) abort(422)`
- [2026-05-19] | EditableField : `setSaving(true)` est async, donc `onBlur` + `onMouseDown(check)` peuvent tous deux passer la garde `if (saving) return` dans le même tick et lancer 2 saves | toujours doubler `useState(saving)` d'un `useRef(savingRef)` synchrone quand la garde anti-double-click doit fonctionner DANS le même render. Le state React garantit le visuel, le ref garantit la logique
- [2026-05-19] | EditableField : `useEffect([value])` qui fait `setDraft(value)` écrasait ce que l'utilisateur tape pendant un refetch parent | toute synchronisation `state <- prop` doit être gardée par `if (!isEditing)`. Pattern : un `editingRef` (mirror du state via useEffect) lu dans le useEffect de sync pour éviter le timing async de useState
- [2026-05-19] | EditableField : catch silencieux -> l'utilisateur croit que ça a marché alors que la sauvegarde a échoué (CRIT UX) | TOUS les catch d'opérations de sauvegarde DOIVENT au minimum afficher un toast.error + restorer la valeur d'origine. JAMAIS de `catch {}` vide, même avec un commentaire « erreur silencieuse »
- [2026-05-19] | Création d'exception sur récurrence : validateur n'acceptait que start/end/employee/status/comment, tous les autres champs (key_id, address_id, contact_id, transport_mode, vehicle_type, bill_client, is_paid, internal_comment) étaient perdus silencieusement | quand on a 2 chemins de mutation (update normal + create exception), TOUJOURS aligner la validation des deux. L'utilisateur ne sait pas qu'il édite une "exception" : son geste UI est identique, le payload doit l'être aussi
- [2026-05-19] | Observer fallback sur `auth()->id()` notifiait l'expéditeur lui-même quand assigned_to et owner_user_id étaient null -> spam de notifs auto | JAMAIS de fallback notification sur l'auteur de l'event. Cascade correcte : assigned → owner → super_admins (filtrés pour exclure l'auteur). Si aucun destinataire valide reste : log warning et skip, jamais "se notifier soi-même"
- [2026-05-19] | Suppression de Client/Employee = cascade silencieuse sur interventions futures -> RDV orphelins en BDD (employee_id pointant sur ligne supprimée, ou client_id idem) | TOUJOURS un garde-fou métier avant DELETE : compter les enfants "vivants" (RDV futurs, factures non payées, etc.) et bloquer en 409 si présents. Bypass `?force=1` réservé aux super_admin pour les cas exceptionnels
